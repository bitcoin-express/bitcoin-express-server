#%RAML 1.0 DataType

type: string
default: "3600"
description: !
  Time in seconds after which the transaction in the 'initial' status will be marked as expired.
  If it's not defined neither in the transaction nor on the Merchant's account then default Gateway setting is used.
example: "3600"
