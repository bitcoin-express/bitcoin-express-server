#%RAML 1.0 DataType

type: string
default: "3600"
description: !
  Time in seconds after which the transaction in the _initial_ status will be marked as _expired_.

  If it is not defined neither in the transaction nor on the Merchant's account then the default Gateway's setting will be used.
example: "3600"
