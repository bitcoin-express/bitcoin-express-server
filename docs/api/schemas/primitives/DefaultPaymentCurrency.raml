#%RAML 1.0 DataType

type: !include Currency.raml
description: |
  Default currency for transactions. It will be used by default if there is no explicit currency set for a transaction.
  If it's not defined neither in the transaction nor on the Merchant's account then default Gateway setting is used.
example: "XBT"
