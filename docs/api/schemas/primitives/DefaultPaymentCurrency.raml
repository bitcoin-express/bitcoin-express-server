#%RAML 1.0 DataType

type: !include Currency.raml
description: |
  Default currency for transactions.

  It will be used as default if the currency is not explicitly set for a transaction.
  If it is not defined neither in the transaction nor on the Merchant's account then the default Gateway's setting will be used.
example: "XBT"
